// Generated by scripts/generate-routes.mjs — DO NOT EDIT.
//
// 每个命令一条 handler，POST /api/<command_name>，body 的 JSON key 与
// #[tauri::command] 参数名逐字一致。rename_all 逐命令镜像 tauri 属性。
#![allow(unused_imports, clippy::too_many_arguments)]
use axum::extract::State;
use axum::response::Response;
use axum::routing::post;
use axum::Router;
use serde::Deserialize;
use serde_json::Value;
use std::collections::HashMap;
// 不用 axum::Json：它的提取失败是 422/415 纯文本，违反「所有失败都是
// 400 + {error}」的契约（routes.rs）。crate::json::Json 把它们折进同一形状。
use crate::json::Json;
use crate::routes::respond;
use crate::state::AppState;

mod automation_claim_prompt_runs {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_claim_prompt_runs(&state.automation_store).await)
    }
}

mod automation_clear_runs {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationClearRunsRouteArgs {
        task_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationClearRunsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_clear_runs(args.task_id, &state.automation_store).await)
    }
}

mod automation_complete_prompt_run {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationCompletePromptRunRouteArgs {
        input: CompletePromptRunInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationCompletePromptRunRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_complete_prompt_run(args.input, &state.automation_store).await)
    }
}

mod automation_cron_apply {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationCronApplyRouteArgs {
        input: AutomationApplyInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationCronApplyRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_cron_apply(args.input, &state.automation_store).await)
    }
}

mod automation_hooks_apply {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationHooksApplyRouteArgs {
        input: AutomationApplyInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationHooksApplyRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_hooks_apply(args.input, &state.automation_store).await)
    }
}

mod automation_list_runs {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationListRunsRouteArgs {
        task_id: String,
        limit: Option<usize>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationListRunsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_list_runs(args.task_id, args.limit, &state.automation_store).await)
    }
}

mod automation_release_prompt_run {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationReleasePromptRunRouteArgs {
        execution_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationReleasePromptRunRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_release_prompt_run(args.execution_id, &state.automation_store).await)
    }
}

mod automation_run_cron_now {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct AutomationRunCronNowRouteArgs {
        task_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<AutomationRunCronNowRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_run_cron_now(args.task_id, &state.automation_store).await)
    }
}

mod automation_snapshot {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::cron::automation_snapshot(&state.automation_store).await)
    }
}

mod chat_history_append_segment {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryAppendSegmentRouteArgs {
        input: ChatHistorySegmentMutationInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryAppendSegmentRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_append_segment(args.input, &state.events).await)
    }
}

mod chat_history_branch {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryBranchRouteArgs {
        id: String,
        base_message_ref: ChatHistoryMessageRef,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryBranchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_branch(args.id, args.base_message_ref, &state.events).await)
    }
}

mod chat_history_delete {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryDeleteRouteArgs {
        id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryDeleteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_delete(args.id, &state.events).await)
    }
}

mod chat_history_get_window {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryGetWindowRouteArgs {
        id: String,
        max_messages: i64,
        before_offset: Option<i64>,
        expected_revision: Option<String>,
        include_active_segment: bool,
    }

    pub async fn handle(
    Json(args): Json<ChatHistoryGetWindowRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_get_window(args.id, args.max_messages, args.before_offset, args.expected_revision, args.include_active_segment).await)
    }
}

mod chat_history_list {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryListRouteArgs {
        page: i64,
        page_size: i64,
        cwd: Option<String>,
        cwd_empty: Option<bool>,
    }

    pub async fn handle(
    Json(args): Json<ChatHistoryListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_list(args.page, args.page_size, args.cwd, args.cwd_empty).await)
    }
}

mod chat_history_rename {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryRenameRouteArgs {
        id: String,
        title: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryRenameRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_rename(args.id, args.title, &state.events).await)
    }
}

mod chat_history_replace_from_message {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryReplaceFromMessageRouteArgs {
        id: String,
        base_message_ref: ChatHistoryMessageRef,
        replacement_message: Value,
        max_messages: i64,
        expected_revision: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryReplaceFromMessageRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_replace_from_message(args.id, args.base_message_ref, args.replacement_message, args.max_messages, args.expected_revision, &state.events).await)
    }
}

mod chat_history_search {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistorySearchRouteArgs {
        args: ChatHistorySearchArgs,
    }

    pub async fn handle(
    Json(args): Json<ChatHistorySearchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_search(args.args).await)
    }
}

mod chat_history_set_model {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistorySetModelRouteArgs {
        id: String,
        selected_model_json: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistorySetModelRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_set_model(args.id, args.selected_model_json, &state.events).await)
    }
}

mod chat_history_set_pinned {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistorySetPinnedRouteArgs {
        id: String,
        is_pinned: bool,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistorySetPinnedRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_set_pinned(args.id, args.is_pinned, &state.events).await)
    }
}

mod chat_history_share_get {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryShareGetRouteArgs {
        id: String,
    }

    pub async fn handle(
    Json(args): Json<ChatHistoryShareGetRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_share_get(args.id).await)
    }
}

mod chat_history_share_set {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryShareSetRouteArgs {
        id: String,
        enabled: bool,
        redact_tool_content: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryShareSetRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_share_set(args.id, args.enabled, args.redact_tool_content, &state.events).await)
    }
}

mod chat_history_shared_list {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistorySharedListRouteArgs {
        page: i64,
        page_size: i64,
    }

    pub async fn handle(
    Json(args): Json<ChatHistorySharedListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_shared_list(args.page, args.page_size).await)
    }
}

mod chat_history_upsert {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryUpsertRouteArgs {
        input: ChatHistoryUpsertInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryUpsertRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_upsert(args.input, &state.events).await)
    }
}

mod chat_history_upsert_active_segment {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct ChatHistoryUpsertActiveSegmentRouteArgs {
        input: ChatHistorySegmentMutationInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ChatHistoryUpsertActiveSegmentRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::chat_history::chat_history_upsert_active_segment(args.input, &state.events).await)
    }
}

mod chat_history_workdirs {
    use super::*;
    use agent_core::commands::chat_history::*;
    use agent_core::commands::{history_db, subagent_store};
    use agent_core::events::EventBus;
    use agent_core::services::memory::{MemoryHistorySearchMatch, MemorySearchArgs};

    pub async fn handle() -> Response {
        respond(agent_core::commands::chat_history::chat_history_workdirs().await)
    }
}

mod cron_validate_expression {
    use super::*;
    use agent_core::commands::cron::*;
    use agent_core::services::automation::{
    validate_cron_expression, AutomationApplyInput, AutomationSnapshot, AutomationStore,
    CompletePromptRunInput, CronApplyResponse, CronRunNowResponse, CronRunRecord,
    HooksApplyResponse, PromptCompletionResponse, PromptRunRequest,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct CronValidateExpressionRouteArgs {
        expression: String,
    }

    pub async fn handle(
    Json(args): Json<CronValidateExpressionRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::cron::cron_validate_expression(args.expression).await)
    }
}

mod fs_create_dir {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsCreateDirRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<FsCreateDirRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_create_dir(args.workdir, args.path).await)
    }
}

mod fs_delete {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsDeleteRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<FsDeleteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_delete(args.workdir, args.path).await)
    }
}

mod fs_edit_text {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsEditTextRouteArgs {
        workdir: String,
        path: String,
        old_string: String,
        new_string: String,
        expected_replacements: Option<usize>,
        replace_all: Option<bool>,
        expected_mtime_ms: Option<u64>,
        expected_content_hash: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<FsEditTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_edit_text(args.workdir, args.path, args.old_string, args.new_string, args.expected_replacements, args.replace_all, args.expected_mtime_ms, args.expected_content_hash).await)
    }
}

mod fs_glob {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsGlobRouteArgs {
        workdir: String,
        path: Option<String>,
        pattern: String,
        offset: Option<usize>,
        max_results: Option<usize>,
        sort_by: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<FsGlobRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_glob(args.workdir, args.path, args.pattern, args.offset, args.max_results, args.sort_by).await)
    }
}

mod fs_grep {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsGrepRouteArgs {
        workdir: String,
        path: Option<String>,
        pattern: String,
        file_pattern: Option<String>,
        ignore_case: Option<bool>,
        output_mode: Option<String>,
        head_limit: Option<usize>,
        offset: Option<usize>,
        context: Option<usize>,
        multiline: Option<bool>,
    }

    pub async fn handle(
    Json(args): Json<FsGrepRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_grep(args.workdir, args.path, args.pattern, args.file_pattern, args.ignore_case, args.output_mode, args.head_limit, args.offset, args.context, args.multiline).await)
    }
}

mod fs_list {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsListRouteArgs {
        workdir: String,
        path: Option<String>,
        depth: Option<usize>,
        offset: Option<usize>,
        max_results: Option<usize>,
        show_hidden: Option<bool>,
    }

    pub async fn handle(
    Json(args): Json<FsListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_list(args.workdir, args.path, args.depth, args.offset, args.max_results, args.show_hidden).await)
    }
}

mod fs_list_dirs {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsListDirsRouteArgs {
        path: String,
        max_results: Option<usize>,
    }

    pub async fn handle(
    Json(args): Json<FsListDirsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_list_dirs(args.path, args.max_results).await)
    }
}

mod fs_mention_list {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsMentionListRouteArgs {
        workdir: String,
        max_results: Option<usize>,
        query: Option<String>,
        show_hidden: Option<bool>,
    }

    pub async fn handle(
    Json(args): Json<FsMentionListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_mention_list(args.workdir, args.max_results, args.query, args.show_hidden).await)
    }
}

mod fs_path_status {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsPathStatusRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<FsPathStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_path_status(args.workdir, args.path).await)
    }
}

mod fs_read_editable_text {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsReadEditableTextRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<FsReadEditableTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_read_editable_text(args.workdir, args.path).await)
    }
}

mod fs_read_image_source {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct FsReadImageSourceRouteArgs {
        workdir: String,
        source: String,
        source_type: Option<String>,
        mime_type: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<FsReadImageSourceRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_read_image_source(args.workdir, args.source, args.source_type, args.mime_type).await)
    }
}

mod fs_read_text {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsReadTextRouteArgs {
        workdir: String,
        path: String,
        start_line: Option<usize>,
        limit: Option<usize>,
        page_start: Option<usize>,
        page_limit: Option<usize>,
        cell_start: Option<usize>,
        cell_limit: Option<usize>,
    }

    pub async fn handle(
    Json(args): Json<FsReadTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_read_text(args.workdir, args.path, args.start_line, args.limit, args.page_start, args.page_limit, args.cell_start, args.cell_limit).await)
    }
}

mod fs_read_workspace_image {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsReadWorkspaceImageRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<FsReadWorkspaceImageRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_read_workspace_image(args.workdir, args.path).await)
    }
}

mod fs_rename {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsRenameRouteArgs {
        workdir: String,
        from_path: String,
        to_path: String,
    }

    pub async fn handle(
    Json(args): Json<FsRenameRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_rename(args.workdir, args.from_path, args.to_path).await)
    }
}

mod fs_roots {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    pub async fn handle() -> Response {
        respond(agent_core::commands::fs::fs_roots().await)
    }
}

mod fs_write_text {
    use super::*;
    use agent_core::commands::fs::*;
    use agent_core::runtime::platform::expand_tilde_path;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct FsWriteTextRouteArgs {
        workdir: String,
        path: String,
        content: String,
        mode: String,
        expected_mtime_ms: Option<u64>,
        expected_content_hash: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<FsWriteTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::fs::fs_write_text(args.workdir, args.path, args.content, args.mode, args.expected_mtime_ms, args.expected_content_hash).await)
    }
}

mod git_add_to_gitignore {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitAddToGitignoreRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<GitAddToGitignoreRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_add_to_gitignore(args.workdir, args.path).await)
    }
}

mod git_branches {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitBranchesRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitBranchesRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_branches(args.workdir).await)
    }
}

mod git_clone_repository {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCloneRepositoryRouteArgs {
        parent: String,
        name: String,
        remote_url: String,
        branch: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitCloneRepositoryRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_clone_repository(args.parent, args.name, args.remote_url, args.branch).await)
    }
}

mod git_clone_repository_cancel {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCloneRepositoryCancelRouteArgs {
        task_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<GitCloneRepositoryCancelRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_clone_repository_cancel(&state.git_clone_tasks, args.task_id))
    }
}

mod git_clone_repository_dismiss {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCloneRepositoryDismissRouteArgs {
        task_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<GitCloneRepositoryDismissRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_clone_repository_dismiss(&state.git_clone_tasks, args.task_id))
    }
}

mod git_clone_repository_start {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCloneRepositoryStartRouteArgs {
        parent: String,
        name: String,
        remote_url: String,
        branch: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<GitCloneRepositoryStartRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_clone_repository_start(&state.git_clone_tasks, args.parent, args.name, args.remote_url, args.branch))
    }
}

mod git_clone_repository_tasks {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::git::git_clone_repository_tasks(&state.git_clone_tasks))
    }
}

mod git_commit {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCommitRouteArgs {
        workdir: String,
        message: String,
    }

    pub async fn handle(
    Json(args): Json<GitCommitRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_commit(args.workdir, args.message).await)
    }
}

mod git_commit_details {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCommitDetailsRouteArgs {
        workdir: String,
        commit: String,
    }

    pub async fn handle(
    Json(args): Json<GitCommitDetailsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_commit_details(args.workdir, args.commit).await)
    }
}

mod git_commit_diff {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCommitDiffRouteArgs {
        workdir: String,
        commit: String,
        path: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitCommitDiffRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_commit_diff(args.workdir, args.commit, args.path).await)
    }
}

mod git_compare_commit_with_remote {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCompareCommitWithRemoteRouteArgs {
        workdir: String,
        commit: String,
    }

    pub async fn handle(
    Json(args): Json<GitCompareCommitWithRemoteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_compare_commit_with_remote(args.workdir, args.commit).await)
    }
}

mod git_create_branch {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitCreateBranchRouteArgs {
        workdir: String,
        branch: String,
        start_point: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitCreateBranchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_create_branch(args.workdir, args.branch, args.start_point).await)
    }
}

mod git_delete_branch {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitDeleteBranchRouteArgs {
        workdir: String,
        branch: String,
        force: Option<bool>,
    }

    pub async fn handle(
    Json(args): Json<GitDeleteBranchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_delete_branch(args.workdir, args.branch, args.force).await)
    }
}

mod git_diff {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitDiffRouteArgs {
        workdir: String,
        mode: Option<String>,
        path: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitDiffRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_diff(args.workdir, args.mode, args.path).await)
    }
}

mod git_discard {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitDiscardRouteArgs {
        workdir: String,
        path: String,
        old_path: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitDiscardRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_discard(args.workdir, args.path, args.old_path).await)
    }
}

mod git_discard_all {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitDiscardAllRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitDiscardAllRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_discard_all(args.workdir).await)
    }
}

mod git_discover_repositories {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitDiscoverRepositoriesRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitDiscoverRepositoriesRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_discover_repositories(args.workdir).await)
    }
}

mod git_fetch {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitFetchRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitFetchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_fetch(args.workdir).await)
    }
}

mod git_init {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitInitRouteArgs {
        workdir: String,
        branch: Option<String>,
        user_name: Option<String>,
        user_email: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitInitRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_init(args.workdir, args.branch, args.user_name, args.user_email).await)
    }
}

mod git_list_remote_branches {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitListRemoteBranchesRouteArgs {
        remote_url: String,
    }

    pub async fn handle(
    Json(args): Json<GitListRemoteBranchesRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_list_remote_branches(args.remote_url).await)
    }
}

mod git_log {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitLogRouteArgs {
        workdir: String,
        limit: Option<usize>,
        skip: Option<usize>,
    }

    pub async fn handle(
    Json(args): Json<GitLogRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_log(args.workdir, args.limit, args.skip).await)
    }
}

mod git_pull {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitPullRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitPullRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_pull(args.workdir).await)
    }
}

mod git_push {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitPushRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitPushRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_push(args.workdir).await)
    }
}

mod git_rename_branch {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitRenameBranchRouteArgs {
        workdir: String,
        branch: String,
        new_branch: String,
    }

    pub async fn handle(
    Json(args): Json<GitRenameBranchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_rename_branch(args.workdir, args.branch, args.new_branch).await)
    }
}

mod git_set_remote {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitSetRemoteRouteArgs {
        workdir: String,
        remote_url: String,
    }

    pub async fn handle(
    Json(args): Json<GitSetRemoteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_set_remote(args.workdir, args.remote_url).await)
    }
}

mod git_stage {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitStageRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<GitStageRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_stage(args.workdir, args.path).await)
    }
}

mod git_stage_all {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitStageAllRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitStageAllRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_stage_all(args.workdir).await)
    }
}

mod git_stash_pop {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitStashPopRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitStashPopRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_stash_pop(args.workdir).await)
    }
}

mod git_stash_push {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitStashPushRouteArgs {
        workdir: String,
        message: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitStashPushRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_stash_push(args.workdir, args.message).await)
    }
}

mod git_status {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitStatusRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_status(args.workdir).await)
    }
}

mod git_switch_branch {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitSwitchBranchRouteArgs {
        workdir: String,
        branch: String,
        kind: Option<String>,
    }

    pub async fn handle(
    Json(args): Json<GitSwitchBranchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_switch_branch(args.workdir, args.branch, args.kind).await)
    }
}

mod git_unstage {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitUnstageRouteArgs {
        workdir: String,
        path: String,
    }

    pub async fn handle(
    Json(args): Json<GitUnstageRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_unstage(args.workdir, args.path).await)
    }
}

mod git_unstage_all {
    use super::*;
    use agent_core::commands::git::*;
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
    terminate_process_tree_by_pid,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct GitUnstageAllRouteArgs {
        workdir: String,
    }

    pub async fn handle(
    Json(args): Json<GitUnstageAllRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::git::git_unstage_all(args.workdir).await)
    }
}

mod hook_cancel_scope {
    use super::*;
    use agent_core::commands::hook::*;
    use agent_core::runtime::shell_runner::{
    run_shell_script_with_envs, ShellCancelFlag, ShellCancelToken, ShellRunResponse,
};
    use agent_core::runtime::task_runner::{
    build_http_client, resolve_workdir, run_single_http_request, HttpRequestInput,
};
    use agent_core::services::automation::validate::{MAX_HOOK_TIMEOUT_MS, MIN_HOOK_TIMEOUT_MS};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct HookCancelScopeRouteArgs {
        scope_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<HookCancelScopeRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::hook::hook_cancel_scope(args.scope_id, &state.hook_scopes).await)
    }
}

mod hook_run_http_requests {
    use super::*;
    use agent_core::commands::hook::*;
    use agent_core::runtime::shell_runner::{
    run_shell_script_with_envs, ShellCancelFlag, ShellCancelToken, ShellRunResponse,
};
    use agent_core::runtime::task_runner::{
    build_http_client, resolve_workdir, run_single_http_request, HttpRequestInput,
};
    use agent_core::services::automation::validate::{MAX_HOOK_TIMEOUT_MS, MIN_HOOK_TIMEOUT_MS};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct HookRunHttpRequestsRouteArgs {
        requests: Vec<HttpRequestInput>,
        scope_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<HookRunHttpRequestsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::hook::hook_run_http_requests(args.requests, args.scope_id, &state.hook_scopes).await)
    }
}

mod hook_run_script {
    use super::*;
    use agent_core::commands::hook::*;
    use agent_core::runtime::shell_runner::{
    run_shell_script_with_envs, ShellCancelFlag, ShellCancelToken, ShellRunResponse,
};
    use agent_core::runtime::task_runner::{
    build_http_client, resolve_workdir, run_single_http_request, HttpRequestInput,
};
    use agent_core::services::automation::validate::{MAX_HOOK_TIMEOUT_MS, MIN_HOOK_TIMEOUT_MS};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct HookRunScriptRouteArgs {
        workdir: Option<String>,
        script: String,
        timeout_ms: Option<u64>,
        scope_id: Option<String>,
        context: Option<HashMap<String, String>>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<HookRunScriptRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::hook::hook_run_script(args.workdir, args.script, args.timeout_ms, args.scope_id, args.context, &state.hook_scopes).await)
    }
}

mod managed_process_clear {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ManagedProcessClearRouteArgs {
        process_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ManagedProcessClearRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_clear(&state.managed_processes, args.process_id))
    }
}

mod managed_process_read_log {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ManagedProcessReadLogRouteArgs {
        process_id: String,
        max_bytes: Option<u64>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ManagedProcessReadLogRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_read_log(&state.managed_processes, args.process_id, args.max_bytes))
    }
}

mod managed_process_snapshot {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_snapshot(&state.managed_processes))
    }
}

mod managed_process_start {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ManagedProcessStartRouteArgs {
        workdir: String,
        command: String,
        cwd: Option<String>,
        label: Option<String>,
        isolated: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ManagedProcessStartRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_start(&state.managed_processes, args.workdir, args.command, args.cwd, args.label, args.isolated))
    }
}

mod managed_process_status {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ManagedProcessStatusRouteArgs {
        process_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ManagedProcessStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_status(&state.managed_processes, args.process_id))
    }
}

mod managed_process_stop {
    use super::*;
    use agent_core::commands::process::*;
    use agent_core::runtime::managed_process::{
    ManagedProcessLogResponse, ManagedProcessRegistry, ManagedProcessSnapshot,
    ManagedProcessStartResponse, ManagedProcessStatusResponse, ManagedProcessStopResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ManagedProcessStopRouteArgs {
        process_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ManagedProcessStopRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::process::managed_process_stop(&state.managed_processes, args.process_id))
    }
}

mod mcp_call_tool {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpCallToolRouteArgs {
        server_id: String,
        tool_name: String,
        arguments: Value,
        run_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpCallToolRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_call_tool(&state.mcp, &state.shell_runs, args.server_id, args.tool_name, args.arguments, args.run_id).await)
    }
}

mod mcp_list_tools {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpListToolsRouteArgs {
        servers: Vec<McpServerConfig>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpListToolsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_list_tools(&state.mcp, args.servers).await)
    }
}

mod mcp_restart_server {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpRestartServerRouteArgs {
        server: McpServerConfig,
        include_schema: Option<bool>,
        persist: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpRestartServerRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_restart_server(&state.mcp, args.server, args.include_schema, args.persist).await)
    }
}

mod mcp_runtime_status {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpRuntimeStatusRouteArgs {
        server_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpRuntimeStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_runtime_status(&state.mcp, args.server_id).await)
    }
}

mod mcp_stop_server {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpStopServerRouteArgs {
        server_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpStopServerRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_stop_server(&state.mcp, args.server_id).await)
    }
}

mod mcp_test_server {
    use super::*;
    use agent_core::commands::mcp::*;
    use agent_core::runtime::platform::{
    expand_tilde_path, maybe_augment_macos_path, resolve_program_path_with_current_dir,
};
    use agent_core::runtime::process::{
    configure_child_process_group, kill_child_process_tree_best_effort,
};
    use agent_core::runtime::shell_runner::ShellRunRegistry;

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct McpTestServerRouteArgs {
        server: McpServerConfig,
        include_schema: Option<bool>,
        persist: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<McpTestServerRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::mcp::mcp_test_server(&state.mcp, args.server, args.include_schema, args.persist).await)
    }
}

mod memory_accept {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryAcceptRouteArgs {
        args: MemoryAcceptArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryAcceptRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_accept(&state.memory_store, args.args).await)
    }
}

mod memory_apply_batch {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryApplyBatchRouteArgs {
        args: MemoryBatchArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryApplyBatchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_apply_batch(&state.memory_store, args.args).await)
    }
}

mod memory_delete {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryDeleteRouteArgs {
        args: MemoryDeleteArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryDeleteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_delete(&state.memory_store, args.args).await)
    }
}

mod memory_delete_project {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryDeleteProjectRouteArgs {
        args: MemoryDeleteProjectArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryDeleteProjectRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_delete_project(&state.memory_store, args.args).await)
    }
}

mod memory_index_overview {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryIndexOverviewRouteArgs {
        workdir: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryIndexOverviewRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_index_overview(&state.memory_store, args.workdir).await)
    }
}

mod memory_list {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryListRouteArgs {
        args: MemoryListArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_list(&state.memory_store, args.args).await)
    }
}

mod memory_organize_due_claim {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeDueClaimRouteArgs {
        args: MemoryOrganizeDueClaimArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeDueClaimRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_due_claim(&state.memory_store, args.args).await)
    }
}

mod memory_organize_due_complete {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeDueCompleteRouteArgs {
        args: MemoryOrganizeRunUpdateArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeDueCompleteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_due_complete(&state.memory_store, args.args).await)
    }
}

mod memory_organize_run_clear_history {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_run_clear_history(&state.memory_store).await)
    }
}

mod memory_organize_run_create {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeRunCreateRouteArgs {
        args: MemoryOrganizeRunCreateArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeRunCreateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_run_create(&state.memory_store, args.args).await)
    }
}

mod memory_organize_run_list {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeRunListRouteArgs {
        args: Option<MemoryOrganizeRunListArgs>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeRunListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_run_list(&state.memory_store, args.args).await)
    }
}

mod memory_organize_run_read {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeRunReadRouteArgs {
        args: MemoryOrganizeRunReadArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeRunReadRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_run_read(&state.memory_store, args.args).await)
    }
}

mod memory_organize_run_update {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryOrganizeRunUpdateRouteArgs {
        args: MemoryOrganizeRunUpdateArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryOrganizeRunUpdateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_organize_run_update(&state.memory_store, args.args).await)
    }
}

mod memory_paths_info {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_paths_info(&state.memory_store).await)
    }
}

mod memory_quota_summary {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryQuotaSummaryRouteArgs {
        args: Option<MemoryQuotaSummaryArgs>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryQuotaSummaryRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_quota_summary(&state.memory_store, args.args).await)
    }
}

mod memory_read {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryReadRouteArgs {
        args: MemoryReadArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryReadRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_read(&state.memory_store, args.args).await)
    }
}

mod memory_recent_rejections {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryRecentRejectionsRouteArgs {
        args: Option<MemoryRecentRejectionsArgs>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryRecentRejectionsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_recent_rejections(&state.memory_store, args.args).await)
    }
}

mod memory_search {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemorySearchRouteArgs {
        args: MemorySearchArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemorySearchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_search(&state.memory_store, args.args).await)
    }
}

mod memory_today_daily {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryTodayDailyRouteArgs {
        rollover_hour: Option<u32>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryTodayDailyRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_today_daily(&state.memory_store, args.rollover_hour).await)
    }
}

mod memory_today_local_date {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryTodayLocalDateRouteArgs {
        rollover_hour: Option<u32>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryTodayLocalDateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_today_local_date(&state.memory_store, args.rollover_hour).await)
    }
}

mod memory_update {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryUpdateRouteArgs {
        args: MemoryUpdateArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryUpdateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_update(&state.memory_store, args.args).await)
    }
}

mod memory_wipe_all {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_wipe_all(&state.memory_store).await)
    }
}

mod memory_write {
    use super::*;
    use agent_core::commands::chat_history;
    use agent_core::commands::memory::*;
    use agent_core::services::memory::{
    MemoryAcceptArgs, MemoryBatchArgs, MemoryBatchResponse, MemoryDeleteArgs,
    MemoryDeleteProjectArgs, MemoryDeleteProjectResponse, MemoryListArgs, MemoryListResponse,
    MemoryMutationResponse, MemoryOrganizeDueClaimArgs, MemoryOrganizeDueClaimResponse,
    MemoryOrganizeRun, MemoryOrganizeRunClearHistoryResponse, MemoryOrganizeRunCreateArgs,
    MemoryOrganizeRunCreateResponse, MemoryOrganizeRunListArgs, MemoryOrganizeRunListResponse,
    MemoryOrganizeRunReadArgs, MemoryOrganizeRunUpdateArgs, MemoryOverviewResponse,
    MemoryPathsInfo, MemoryQuotaSummaryArgs, MemoryQuotaSummaryResponse, MemoryReadArgs,
    MemoryReadResponse, MemoryRecentRejectionsArgs, MemoryRecentRejectionsResponse,
    MemorySearchArgs, MemorySearchResponse, MemoryStore, MemoryUpdateArgs, MemoryWriteArgs,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct MemoryWriteRouteArgs {
        args: MemoryWriteArgs,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<MemoryWriteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::memory::memory_write(&state.memory_store, args.args).await)
    }
}

mod runtime_cancel {
    use super::*;
    use agent_core::commands::shell::*;
    use agent_core::runtime::shell_runner::{run_shell_script, ShellRunRegistry, ShellRunResponse};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct RuntimeCancelRouteArgs {
        run_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<RuntimeCancelRouteArgs>,
    ) -> Response {
        respond(Ok::<_, String>(agent_core::commands::shell::runtime_cancel(&state.shell_runs, args.run_id)))
    }
}

mod settings_apply_ssh_patch {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsApplySshPatchRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsApplySshPatchRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_apply_ssh_patch(args.payload).await)
    }
}

mod settings_list_ccswitch_providers {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    pub async fn handle() -> Response {
        respond(agent_core::commands::settings::settings_list_ccswitch_providers().await)
    }
}

mod settings_list_cherry_studio_providers {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    pub async fn handle() -> Response {
        respond(agent_core::commands::settings::settings_list_cherry_studio_providers().await)
    }
}

mod settings_list_cherry_studio_providers_from_path {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsListCherryStudioProvidersFromPathRouteArgs {
        data_path: String,
    }

    pub async fn handle(
    Json(args): Json<SettingsListCherryStudioProvidersFromPathRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_list_cherry_studio_providers_from_path(args.data_path).await)
    }
}

mod settings_load_all {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    pub async fn handle() -> Response {
        respond(agent_core::commands::settings::settings_load_all().await)
    }
}

mod settings_reset_ssh_known_host {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsResetSshKnownHostRouteArgs {
        host: String,
        port: u16,
    }

    pub async fn handle(
    Json(args): Json<SettingsResetSshKnownHostRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_reset_ssh_known_host(args.host, args.port).await)
    }
}

mod settings_save_agents {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveAgentsRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsSaveAgentsRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_agents(args.payload).await)
    }
}

mod settings_save_mcp {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveMcpRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsSaveMcpRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_mcp(args.payload).await)
    }
}

mod settings_save_memory {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveMemoryRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsSaveMemoryRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_memory(args.payload).await)
    }
}

mod settings_save_providers {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveProvidersRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsSaveProvidersRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_providers(args.payload).await)
    }
}

mod settings_save_remote {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveRemoteRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SettingsSaveRemoteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_remote(args.payload, &state.events).await)
    }
}

mod settings_save_ssh {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveSshRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    Json(args): Json<SettingsSaveSshRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_ssh(args.payload).await)
    }
}

mod settings_save_system {
    use super::*;
    use agent_core::commands::settings::*;
    use agent_core::events::EventBus;
    use agent_core::runtime::project_path::project_path_key as normalize_project_path_key;
    use agent_core::services::automation::AutomationScheduler;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SettingsSaveSystemRouteArgs {
        payload: Value,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SettingsSaveSystemRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::settings::settings_save_system(args.payload, &state.automation_scheduler).await)
    }
}

mod sftp_cancel_transfer {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpCancelTransferRouteArgs {
        session_id: String,
        transfer_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpCancelTransferRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_cancel_transfer(&state.sftp, args.session_id, args.transfer_id))
    }
}

mod sftp_delete {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpDeleteRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        side: String,
        path: String,
        recursive: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpDeleteRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_delete(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.side, args.path, args.recursive).await)
    }
}

mod sftp_list {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpListRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        side: String,
        path: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_list(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.side, args.path).await)
    }
}

mod sftp_mkdir {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpMkdirRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        side: String,
        path: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpMkdirRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_mkdir(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.side, args.path).await)
    }
}

mod sftp_read_text {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpReadTextRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        path: String,
        offset: Option<u64>,
        max_bytes: Option<usize>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpReadTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_read_text(&state.sftp, args.session_id, args.project_path_key, args.path, args.offset, args.max_bytes).await)
    }
}

mod sftp_rename {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpRenameRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        side: String,
        from_path: String,
        to_path: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpRenameRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_rename(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.side, args.from_path, args.to_path).await)
    }
}

mod sftp_stat {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpStatRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        side: String,
        path: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpStatRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_stat(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.side, args.path).await)
    }
}

mod sftp_transfer {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpTransferRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        workdir: String,
        direction: String,
        source_path: String,
        target_path: String,
        recursive: Option<bool>,
        overwrite: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpTransferRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_transfer(&state.sftp, args.session_id, args.project_path_key, args.workdir, args.direction, args.source_path, args.target_path, args.recursive, args.overwrite).await)
    }
}

mod sftp_transfer_status {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpTransferStatusRouteArgs {
        session_id: String,
        transfer_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpTransferStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_transfer_status(&state.sftp, args.session_id, args.transfer_id))
    }
}

mod sftp_write_text {
    use super::*;
    use agent_core::commands::sftp::*;
    use agent_core::runtime::sftp::{
    SftpActionResponse, SftpListResponse, SftpReadTextResponse, SftpSessionRegistry,
    SftpStatResponse, SftpTransferResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SftpWriteTextRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        path: String,
        content: String,
        overwrite: Option<bool>,
        create_parent_dirs: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SftpWriteTextRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::sftp::sftp_write_text(&state.sftp, args.session_id, args.project_path_key, args.path, args.content, args.overwrite, args.create_parent_dirs).await)
    }
}

mod shell_run {
    use super::*;
    use agent_core::commands::shell::*;
    use agent_core::runtime::shell_runner::{run_shell_script, ShellRunRegistry, ShellRunResponse};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct ShellRunRouteArgs {
        workdir: String,
        command: String,
        cwd: Option<String>,
        timeout_ms: Option<u64>,
        max_timeout_ms: Option<u64>,
        provider_id: Option<String>,
        run_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<ShellRunRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::shell::shell_run(&state.shell_runs, args.workdir, args.command, args.cwd, args.timeout_ms, args.max_timeout_ms, args.provider_id, args.run_id).await)
    }
}

mod ssh_terminal_tab_close {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SshTerminalTabCloseRouteArgs {
        tab_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SshTerminalTabCloseRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::ssh_terminal_tab_close(&state.terminals, args.tab_id))
    }
}

mod ssh_terminal_tab_open {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SshTerminalTabOpenRouteArgs {
        session_id: String,
        kind: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SshTerminalTabOpenRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::ssh_terminal_tab_open(&state.terminals, args.session_id, args.kind))
    }
}

mod ssh_terminal_tabs_list {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct SshTerminalTabsListRouteArgs {
        project_path_key: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<SshTerminalTabsListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::ssh_terminal_tabs_list(&state.terminals, args.project_path_key))
    }
}

mod subagent_identity_list {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentIdentityListRouteArgs {
        input: SubagentIdentityListInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentIdentityListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_identity_list(args.input).await)
    }
}

mod subagent_identity_upsert {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentIdentityUpsertRouteArgs {
        input: SubagentIdentityUpsertInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentIdentityUpsertRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_identity_upsert(args.input).await)
    }
}

mod subagent_message_append {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentMessageAppendRouteArgs {
        input: SubagentMessageAppendInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentMessageAppendRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_message_append(args.input).await)
    }
}

mod subagent_message_list {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentMessageListRouteArgs {
        input: SubagentMessageListInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentMessageListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_message_list(args.input).await)
    }
}

mod subagent_run_list {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentRunListRouteArgs {
        input: SubagentRunListInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentRunListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_run_list(args.input).await)
    }
}

mod subagent_run_load {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentRunLoadRouteArgs {
        input: SubagentRunLoadInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentRunLoadRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_run_load(args.input).await)
    }
}

mod subagent_run_prune {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentRunPruneRouteArgs {
        input: SubagentRunPruneInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentRunPruneRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_run_prune(args.input).await)
    }
}

mod subagent_run_save {
    use super::*;
    use agent_core::commands::subagent_store::*;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentRunSaveRouteArgs {
        input: SubagentRunSaveInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentRunSaveRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_store::subagent_run_save(args.input).await)
    }
}

mod subagent_worktree_apply {
    use super::*;
    use agent_core::commands::subagent_worktree::*;
    use agent_core::runtime::process::configure_child_process_group;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentWorktreeApplyRouteArgs {
        input: SubagentWorktreeApplyInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentWorktreeApplyRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_worktree::subagent_worktree_apply(args.input).await)
    }
}

mod subagent_worktree_cleanup {
    use super::*;
    use agent_core::commands::subagent_worktree::*;
    use agent_core::runtime::process::configure_child_process_group;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentWorktreeCleanupRouteArgs {
        input: SubagentWorktreeCleanupInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentWorktreeCleanupRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_worktree::subagent_worktree_cleanup(args.input).await)
    }
}

mod subagent_worktree_create {
    use super::*;
    use agent_core::commands::subagent_worktree::*;
    use agent_core::runtime::process::configure_child_process_group;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentWorktreeCreateRouteArgs {
        input: SubagentWorktreeCreateInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentWorktreeCreateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_worktree::subagent_worktree_create(args.input).await)
    }
}

mod subagent_worktree_status {
    use super::*;
    use agent_core::commands::subagent_worktree::*;
    use agent_core::runtime::process::configure_child_process_group;

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct SubagentWorktreeStatusRouteArgs {
        input: SubagentWorktreeStatusInput,
    }

    pub async fn handle(
    Json(args): Json<SubagentWorktreeStatusRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::subagent_worktree::subagent_worktree_status(args.input).await)
    }
}

mod system_list_skill_files {
    use super::*;
    use agent_core::services::skills::*;

    pub async fn handle() -> Response {
        respond(agent_core::services::skills::system_list_skill_files().await)
    }
}

mod terminal_answer_ssh_prompt {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalAnswerSshPromptRouteArgs {
        prompt_id: String,
        prompt_answer: Option<String>,
        trust_host_key: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalAnswerSshPromptRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_answer_ssh_prompt(&state.terminals, args.prompt_id, args.prompt_answer, args.trust_host_key).await)
    }
}

mod terminal_cancel_ssh_prompt {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalCancelSshPromptRouteArgs {
        prompt_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalCancelSshPromptRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_cancel_ssh_prompt(&state.terminals, args.prompt_id))
    }
}

mod terminal_close {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalCloseRouteArgs {
        session_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalCloseRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_close(&state.terminals, &state.sftp, args.session_id))
    }
}

mod terminal_close_project {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalCloseProjectRouteArgs {
        project_path_key: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalCloseProjectRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_close_project(&state.terminals, &state.sftp, args.project_path_key))
    }
}

mod terminal_create {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalCreateRouteArgs {
        cwd: String,
        project_path_key: Option<String>,
        shell: Option<String>,
        title: Option<String>,
        cols: Option<u16>,
        rows: Option<u16>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalCreateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_create(&state.terminals, args.cwd, args.project_path_key, args.shell, args.title, args.cols, args.rows))
    }
}

mod terminal_create_ssh {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalCreateSshRouteArgs {
        cwd: String,
        project_path_key: Option<String>,
        ssh_host_id: String,
        title: Option<String>,
        cols: Option<u16>,
        rows: Option<u16>,
        sftp_enabled: Option<bool>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalCreateSshRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_create_ssh(&state.terminals, args.cwd, args.project_path_key, args.ssh_host_id, args.title, args.cols, args.rows, args.sftp_enabled).await)
    }
}

mod terminal_list {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalListRouteArgs {
        project_path_key: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalListRouteArgs>,
    ) -> Response {
        respond(Ok::<_, String>(agent_core::commands::terminal::terminal_list(&state.terminals, args.project_path_key)))
    }
}

mod terminal_read_tail {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalReadTailRouteArgs {
        project_path_key: String,
        session_id: Option<String>,
        max_bytes: Option<usize>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalReadTailRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_read_tail(&state.terminals, args.project_path_key, args.session_id, args.max_bytes))
    }
}

mod terminal_rename {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalRenameRouteArgs {
        session_id: String,
        title: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalRenameRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_rename(&state.terminals, args.session_id, args.title))
    }
}

mod terminal_shell_options {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    pub async fn handle() -> Response {
        respond(Ok::<_, String>(agent_core::commands::terminal::terminal_shell_options()))
    }
}

mod terminal_ssh_exec {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshExecRouteArgs {
        session_id: String,
        command: String,
        cwd: Option<String>,
        timeout_ms: Option<u64>,
        max_bytes: Option<usize>,
        run_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshExecRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_exec(&state.terminals, &state.shell_runs, args.session_id, args.command, args.cwd, args.timeout_ms, args.max_bytes, args.run_id).await)
    }
}

mod terminal_ssh_latency {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshLatencyRouteArgs {
        session_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshLatencyRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_latency(&state.terminals, args.session_id).await)
    }
}

mod terminal_ssh_local_forward_check_port {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshLocalForwardCheckPortRouteArgs {
        local_port: u32,
    }

    pub async fn handle(
    Json(args): Json<TerminalSshLocalForwardCheckPortRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_local_forward_check_port(args.local_port).await)
    }
}

mod terminal_ssh_local_forward_list {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshLocalForwardListRouteArgs {
        session_id: Option<String>,
        project_path_key: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshLocalForwardListRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_local_forward_list(&state.terminals, args.session_id, args.project_path_key))
    }
}

mod terminal_ssh_local_forward_start {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshLocalForwardStartRouteArgs {
        session_id: String,
        project_path_key: Option<String>,
        remote_host: String,
        remote_port: u32,
        local_port: Option<u32>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshLocalForwardStartRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_local_forward_start(&state.terminals, args.session_id, args.project_path_key, args.remote_host, args.remote_port, args.local_port).await)
    }
}

mod terminal_ssh_local_forward_stop {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshLocalForwardStopRouteArgs {
        forward_id: String,
        session_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshLocalForwardStopRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_local_forward_stop(&state.terminals, args.forward_id, args.session_id).await)
    }
}

mod terminal_ssh_reconnect {
    use super::*;
    use agent_core::commands::terminal::*;
    use agent_core::runtime::sftp::SftpSessionRegistry;
    use agent_core::runtime::shell_runner::ShellRunRegistry;
    use agent_core::runtime::terminal::{
    normalize_ssh_local_forward_local_port, ssh_local_forward_local_port_available,
    terminal_shell_options as runtime_terminal_shell_options, SshLocalForwardActionResponse,
    SshLocalForwardListResponse, SshTerminalTabsSnapshot, TerminalListResponse,
    TerminalReadTailResponse, TerminalSessionRecord, TerminalSessionRegistry,
    TerminalShellOptionsResponse, TerminalSnapshotResponse, TerminalSshCreateResponse,
    TerminalSshExecResponse, TerminalSshLatencyResponse, TerminalStreamSnapshotResponse,
};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TerminalSshReconnectRouteArgs {
        session_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TerminalSshReconnectRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::terminal::terminal_ssh_reconnect(&state.terminals, args.session_id).await)
    }
}

mod tunnel_check {
    use super::*;
    use agent_core::services::tunnel::{TunnelCreateInput, TunnelState, TunnelStore, TunnelUpdateInput};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TunnelCheckRouteArgs {
        tunnel_id: Option<String>,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TunnelCheckRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::tunnel::tunnel_check(args.tunnel_id, &state.tunnels).await)
    }
}

mod tunnel_close {
    use super::*;
    use agent_core::services::tunnel::{TunnelCreateInput, TunnelState, TunnelStore, TunnelUpdateInput};

    #[derive(Deserialize)]
    #[serde(rename_all = "snake_case")]
    pub struct TunnelCloseRouteArgs {
        tunnel_id: String,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TunnelCloseRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::tunnel::tunnel_close(args.tunnel_id, &state.tunnels).await)
    }
}

mod tunnel_create {
    use super::*;
    use agent_core::services::tunnel::{TunnelCreateInput, TunnelState, TunnelStore, TunnelUpdateInput};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct TunnelCreateRouteArgs {
        input: TunnelCreateInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TunnelCreateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::tunnel::tunnel_create(args.input, &state.tunnels).await)
    }
}

mod tunnel_state {
    use super::*;
    use agent_core::services::tunnel::{TunnelCreateInput, TunnelState, TunnelStore, TunnelUpdateInput};

    pub async fn handle(
    State(state): State<AppState>,
    ) -> Response {
        respond(agent_core::commands::tunnel::tunnel_state(&state.tunnels))
    }
}

mod tunnel_update {
    use super::*;
    use agent_core::services::tunnel::{TunnelCreateInput, TunnelState, TunnelStore, TunnelUpdateInput};

    #[derive(Deserialize)]
    #[serde(rename_all = "camelCase")]
    pub struct TunnelUpdateRouteArgs {
        input: TunnelUpdateInput,
    }

    pub async fn handle(
    State(state): State<AppState>,
    Json(args): Json<TunnelUpdateRouteArgs>,
    ) -> Response {
        respond(agent_core::commands::tunnel::tunnel_update(args.input, &state.tunnels).await)
    }
}

pub fn gen_router() -> Router<AppState> {
    Router::new()
        .route("/automation_claim_prompt_runs", post(automation_claim_prompt_runs::handle))
        .route("/automation_clear_runs", post(automation_clear_runs::handle))
        .route("/automation_complete_prompt_run", post(automation_complete_prompt_run::handle))
        .route("/automation_cron_apply", post(automation_cron_apply::handle))
        .route("/automation_hooks_apply", post(automation_hooks_apply::handle))
        .route("/automation_list_runs", post(automation_list_runs::handle))
        .route("/automation_release_prompt_run", post(automation_release_prompt_run::handle))
        .route("/automation_run_cron_now", post(automation_run_cron_now::handle))
        .route("/automation_snapshot", post(automation_snapshot::handle))
        .route("/chat_history_append_segment", post(chat_history_append_segment::handle))
        .route("/chat_history_branch", post(chat_history_branch::handle))
        .route("/chat_history_delete", post(chat_history_delete::handle))
        .route("/chat_history_get_window", post(chat_history_get_window::handle))
        .route("/chat_history_list", post(chat_history_list::handle))
        .route("/chat_history_rename", post(chat_history_rename::handle))
        .route("/chat_history_replace_from_message", post(chat_history_replace_from_message::handle))
        .route("/chat_history_search", post(chat_history_search::handle))
        .route("/chat_history_set_model", post(chat_history_set_model::handle))
        .route("/chat_history_set_pinned", post(chat_history_set_pinned::handle))
        .route("/chat_history_share_get", post(chat_history_share_get::handle))
        .route("/chat_history_share_set", post(chat_history_share_set::handle))
        .route("/chat_history_shared_list", post(chat_history_shared_list::handle))
        .route("/chat_history_upsert", post(chat_history_upsert::handle))
        .route("/chat_history_upsert_active_segment", post(chat_history_upsert_active_segment::handle))
        .route("/chat_history_workdirs", post(chat_history_workdirs::handle))
        .route("/cron_validate_expression", post(cron_validate_expression::handle))
        .route("/fs_create_dir", post(fs_create_dir::handle))
        .route("/fs_delete", post(fs_delete::handle))
        .route("/fs_edit_text", post(fs_edit_text::handle))
        .route("/fs_glob", post(fs_glob::handle))
        .route("/fs_grep", post(fs_grep::handle))
        .route("/fs_list", post(fs_list::handle))
        .route("/fs_list_dirs", post(fs_list_dirs::handle))
        .route("/fs_mention_list", post(fs_mention_list::handle))
        .route("/fs_path_status", post(fs_path_status::handle))
        .route("/fs_read_editable_text", post(fs_read_editable_text::handle))
        .route("/fs_read_image_source", post(fs_read_image_source::handle))
        .route("/fs_read_text", post(fs_read_text::handle))
        .route("/fs_read_workspace_image", post(fs_read_workspace_image::handle))
        .route("/fs_rename", post(fs_rename::handle))
        .route("/fs_roots", post(fs_roots::handle))
        .route("/fs_write_text", post(fs_write_text::handle))
        .route("/git_add_to_gitignore", post(git_add_to_gitignore::handle))
        .route("/git_branches", post(git_branches::handle))
        .route("/git_clone_repository", post(git_clone_repository::handle))
        .route("/git_clone_repository_cancel", post(git_clone_repository_cancel::handle))
        .route("/git_clone_repository_dismiss", post(git_clone_repository_dismiss::handle))
        .route("/git_clone_repository_start", post(git_clone_repository_start::handle))
        .route("/git_clone_repository_tasks", post(git_clone_repository_tasks::handle))
        .route("/git_commit", post(git_commit::handle))
        .route("/git_commit_details", post(git_commit_details::handle))
        .route("/git_commit_diff", post(git_commit_diff::handle))
        .route("/git_compare_commit_with_remote", post(git_compare_commit_with_remote::handle))
        .route("/git_create_branch", post(git_create_branch::handle))
        .route("/git_delete_branch", post(git_delete_branch::handle))
        .route("/git_diff", post(git_diff::handle))
        .route("/git_discard", post(git_discard::handle))
        .route("/git_discard_all", post(git_discard_all::handle))
        .route("/git_discover_repositories", post(git_discover_repositories::handle))
        .route("/git_fetch", post(git_fetch::handle))
        .route("/git_init", post(git_init::handle))
        .route("/git_list_remote_branches", post(git_list_remote_branches::handle))
        .route("/git_log", post(git_log::handle))
        .route("/git_pull", post(git_pull::handle))
        .route("/git_push", post(git_push::handle))
        .route("/git_rename_branch", post(git_rename_branch::handle))
        .route("/git_set_remote", post(git_set_remote::handle))
        .route("/git_stage", post(git_stage::handle))
        .route("/git_stage_all", post(git_stage_all::handle))
        .route("/git_stash_pop", post(git_stash_pop::handle))
        .route("/git_stash_push", post(git_stash_push::handle))
        .route("/git_status", post(git_status::handle))
        .route("/git_switch_branch", post(git_switch_branch::handle))
        .route("/git_unstage", post(git_unstage::handle))
        .route("/git_unstage_all", post(git_unstage_all::handle))
        .route("/hook_cancel_scope", post(hook_cancel_scope::handle))
        .route("/hook_run_http_requests", post(hook_run_http_requests::handle))
        .route("/hook_run_script", post(hook_run_script::handle))
        .route("/managed_process_clear", post(managed_process_clear::handle))
        .route("/managed_process_read_log", post(managed_process_read_log::handle))
        .route("/managed_process_snapshot", post(managed_process_snapshot::handle))
        .route("/managed_process_start", post(managed_process_start::handle))
        .route("/managed_process_status", post(managed_process_status::handle))
        .route("/managed_process_stop", post(managed_process_stop::handle))
        .route("/mcp_call_tool", post(mcp_call_tool::handle))
        .route("/mcp_list_tools", post(mcp_list_tools::handle))
        .route("/mcp_restart_server", post(mcp_restart_server::handle))
        .route("/mcp_runtime_status", post(mcp_runtime_status::handle))
        .route("/mcp_stop_server", post(mcp_stop_server::handle))
        .route("/mcp_test_server", post(mcp_test_server::handle))
        .route("/memory_accept", post(memory_accept::handle))
        .route("/memory_apply_batch", post(memory_apply_batch::handle))
        .route("/memory_delete", post(memory_delete::handle))
        .route("/memory_delete_project", post(memory_delete_project::handle))
        .route("/memory_index_overview", post(memory_index_overview::handle))
        .route("/memory_list", post(memory_list::handle))
        .route("/memory_organize_due_claim", post(memory_organize_due_claim::handle))
        .route("/memory_organize_due_complete", post(memory_organize_due_complete::handle))
        .route("/memory_organize_run_clear_history", post(memory_organize_run_clear_history::handle))
        .route("/memory_organize_run_create", post(memory_organize_run_create::handle))
        .route("/memory_organize_run_list", post(memory_organize_run_list::handle))
        .route("/memory_organize_run_read", post(memory_organize_run_read::handle))
        .route("/memory_organize_run_update", post(memory_organize_run_update::handle))
        .route("/memory_paths_info", post(memory_paths_info::handle))
        .route("/memory_quota_summary", post(memory_quota_summary::handle))
        .route("/memory_read", post(memory_read::handle))
        .route("/memory_recent_rejections", post(memory_recent_rejections::handle))
        .route("/memory_search", post(memory_search::handle))
        .route("/memory_today_daily", post(memory_today_daily::handle))
        .route("/memory_today_local_date", post(memory_today_local_date::handle))
        .route("/memory_update", post(memory_update::handle))
        .route("/memory_wipe_all", post(memory_wipe_all::handle))
        .route("/memory_write", post(memory_write::handle))
        .route("/runtime_cancel", post(runtime_cancel::handle))
        .route("/settings_apply_ssh_patch", post(settings_apply_ssh_patch::handle))
        .route("/settings_list_ccswitch_providers", post(settings_list_ccswitch_providers::handle))
        .route("/settings_list_cherry_studio_providers", post(settings_list_cherry_studio_providers::handle))
        .route("/settings_list_cherry_studio_providers_from_path", post(settings_list_cherry_studio_providers_from_path::handle))
        .route("/settings_load_all", post(settings_load_all::handle))
        .route("/settings_reset_ssh_known_host", post(settings_reset_ssh_known_host::handle))
        .route("/settings_save_agents", post(settings_save_agents::handle))
        .route("/settings_save_mcp", post(settings_save_mcp::handle))
        .route("/settings_save_memory", post(settings_save_memory::handle))
        .route("/settings_save_providers", post(settings_save_providers::handle))
        .route("/settings_save_remote", post(settings_save_remote::handle))
        .route("/settings_save_ssh", post(settings_save_ssh::handle))
        .route("/settings_save_system", post(settings_save_system::handle))
        .route("/sftp_cancel_transfer", post(sftp_cancel_transfer::handle))
        .route("/sftp_delete", post(sftp_delete::handle))
        .route("/sftp_list", post(sftp_list::handle))
        .route("/sftp_mkdir", post(sftp_mkdir::handle))
        .route("/sftp_read_text", post(sftp_read_text::handle))
        .route("/sftp_rename", post(sftp_rename::handle))
        .route("/sftp_stat", post(sftp_stat::handle))
        .route("/sftp_transfer", post(sftp_transfer::handle))
        .route("/sftp_transfer_status", post(sftp_transfer_status::handle))
        .route("/sftp_write_text", post(sftp_write_text::handle))
        .route("/shell_run", post(shell_run::handle))
        .route("/ssh_terminal_tab_close", post(ssh_terminal_tab_close::handle))
        .route("/ssh_terminal_tab_open", post(ssh_terminal_tab_open::handle))
        .route("/ssh_terminal_tabs_list", post(ssh_terminal_tabs_list::handle))
        .route("/subagent_identity_list", post(subagent_identity_list::handle))
        .route("/subagent_identity_upsert", post(subagent_identity_upsert::handle))
        .route("/subagent_message_append", post(subagent_message_append::handle))
        .route("/subagent_message_list", post(subagent_message_list::handle))
        .route("/subagent_run_list", post(subagent_run_list::handle))
        .route("/subagent_run_load", post(subagent_run_load::handle))
        .route("/subagent_run_prune", post(subagent_run_prune::handle))
        .route("/subagent_run_save", post(subagent_run_save::handle))
        .route("/subagent_worktree_apply", post(subagent_worktree_apply::handle))
        .route("/subagent_worktree_cleanup", post(subagent_worktree_cleanup::handle))
        .route("/subagent_worktree_create", post(subagent_worktree_create::handle))
        .route("/subagent_worktree_status", post(subagent_worktree_status::handle))
        .route("/system_list_skill_files", post(system_list_skill_files::handle))
        .route("/terminal_answer_ssh_prompt", post(terminal_answer_ssh_prompt::handle))
        .route("/terminal_cancel_ssh_prompt", post(terminal_cancel_ssh_prompt::handle))
        .route("/terminal_close", post(terminal_close::handle))
        .route("/terminal_close_project", post(terminal_close_project::handle))
        .route("/terminal_create", post(terminal_create::handle))
        .route("/terminal_create_ssh", post(terminal_create_ssh::handle))
        .route("/terminal_list", post(terminal_list::handle))
        .route("/terminal_read_tail", post(terminal_read_tail::handle))
        .route("/terminal_rename", post(terminal_rename::handle))
        .route("/terminal_shell_options", post(terminal_shell_options::handle))
        .route("/terminal_ssh_exec", post(terminal_ssh_exec::handle))
        .route("/terminal_ssh_latency", post(terminal_ssh_latency::handle))
        .route("/terminal_ssh_local_forward_check_port", post(terminal_ssh_local_forward_check_port::handle))
        .route("/terminal_ssh_local_forward_list", post(terminal_ssh_local_forward_list::handle))
        .route("/terminal_ssh_local_forward_start", post(terminal_ssh_local_forward_start::handle))
        .route("/terminal_ssh_local_forward_stop", post(terminal_ssh_local_forward_stop::handle))
        .route("/terminal_ssh_reconnect", post(terminal_ssh_reconnect::handle))
        .route("/tunnel_check", post(tunnel_check::handle))
        .route("/tunnel_close", post(tunnel_close::handle))
        .route("/tunnel_create", post(tunnel_create::handle))
        .route("/tunnel_state", post(tunnel_state::handle))
        .route("/tunnel_update", post(tunnel_update::handle))
}

/// 已挂路由的命令名。契约测试拿它和 backend.txt 清单比对，
/// 「新增 command 未加路由」必须导致测试失败。
pub const ROUTED_COMMANDS: &[&str] = &[
    "automation_claim_prompt_runs",
    "automation_clear_runs",
    "automation_complete_prompt_run",
    "automation_cron_apply",
    "automation_hooks_apply",
    "automation_list_runs",
    "automation_release_prompt_run",
    "automation_run_cron_now",
    "automation_snapshot",
    "chat_history_append_segment",
    "chat_history_branch",
    "chat_history_delete",
    "chat_history_get_window",
    "chat_history_list",
    "chat_history_rename",
    "chat_history_replace_from_message",
    "chat_history_search",
    "chat_history_set_model",
    "chat_history_set_pinned",
    "chat_history_share_get",
    "chat_history_share_set",
    "chat_history_shared_list",
    "chat_history_upsert",
    "chat_history_upsert_active_segment",
    "chat_history_workdirs",
    "cron_validate_expression",
    "fs_create_dir",
    "fs_delete",
    "fs_edit_text",
    "fs_glob",
    "fs_grep",
    "fs_list",
    "fs_list_dirs",
    "fs_mention_list",
    "fs_path_status",
    "fs_read_editable_text",
    "fs_read_image_source",
    "fs_read_text",
    "fs_read_workspace_image",
    "fs_rename",
    "fs_roots",
    "fs_write_text",
    "git_add_to_gitignore",
    "git_branches",
    "git_clone_repository",
    "git_clone_repository_cancel",
    "git_clone_repository_dismiss",
    "git_clone_repository_start",
    "git_clone_repository_tasks",
    "git_commit",
    "git_commit_details",
    "git_commit_diff",
    "git_compare_commit_with_remote",
    "git_create_branch",
    "git_delete_branch",
    "git_diff",
    "git_discard",
    "git_discard_all",
    "git_discover_repositories",
    "git_fetch",
    "git_init",
    "git_list_remote_branches",
    "git_log",
    "git_pull",
    "git_push",
    "git_rename_branch",
    "git_set_remote",
    "git_stage",
    "git_stage_all",
    "git_stash_pop",
    "git_stash_push",
    "git_status",
    "git_switch_branch",
    "git_unstage",
    "git_unstage_all",
    "hook_cancel_scope",
    "hook_run_http_requests",
    "hook_run_script",
    "managed_process_clear",
    "managed_process_read_log",
    "managed_process_snapshot",
    "managed_process_start",
    "managed_process_status",
    "managed_process_stop",
    "mcp_call_tool",
    "mcp_list_tools",
    "mcp_restart_server",
    "mcp_runtime_status",
    "mcp_stop_server",
    "mcp_test_server",
    "memory_accept",
    "memory_apply_batch",
    "memory_delete",
    "memory_delete_project",
    "memory_index_overview",
    "memory_list",
    "memory_organize_due_claim",
    "memory_organize_due_complete",
    "memory_organize_run_clear_history",
    "memory_organize_run_create",
    "memory_organize_run_list",
    "memory_organize_run_read",
    "memory_organize_run_update",
    "memory_paths_info",
    "memory_quota_summary",
    "memory_read",
    "memory_recent_rejections",
    "memory_search",
    "memory_today_daily",
    "memory_today_local_date",
    "memory_update",
    "memory_wipe_all",
    "memory_write",
    "runtime_cancel",
    "settings_apply_ssh_patch",
    "settings_list_ccswitch_providers",
    "settings_list_cherry_studio_providers",
    "settings_list_cherry_studio_providers_from_path",
    "settings_load_all",
    "settings_reset_ssh_known_host",
    "settings_save_agents",
    "settings_save_mcp",
    "settings_save_memory",
    "settings_save_providers",
    "settings_save_remote",
    "settings_save_ssh",
    "settings_save_system",
    "sftp_cancel_transfer",
    "sftp_delete",
    "sftp_list",
    "sftp_mkdir",
    "sftp_read_text",
    "sftp_rename",
    "sftp_stat",
    "sftp_transfer",
    "sftp_transfer_status",
    "sftp_write_text",
    "shell_run",
    "ssh_terminal_tab_close",
    "ssh_terminal_tab_open",
    "ssh_terminal_tabs_list",
    "subagent_identity_list",
    "subagent_identity_upsert",
    "subagent_message_append",
    "subagent_message_list",
    "subagent_run_list",
    "subagent_run_load",
    "subagent_run_prune",
    "subagent_run_save",
    "subagent_worktree_apply",
    "subagent_worktree_cleanup",
    "subagent_worktree_create",
    "subagent_worktree_status",
    "system_list_skill_files",
    "terminal_answer_ssh_prompt",
    "terminal_cancel_ssh_prompt",
    "terminal_close",
    "terminal_close_project",
    "terminal_create",
    "terminal_create_ssh",
    "terminal_list",
    "terminal_read_tail",
    "terminal_rename",
    "terminal_shell_options",
    "terminal_ssh_exec",
    "terminal_ssh_latency",
    "terminal_ssh_local_forward_check_port",
    "terminal_ssh_local_forward_list",
    "terminal_ssh_local_forward_start",
    "terminal_ssh_local_forward_stop",
    "terminal_ssh_reconnect",
    "tunnel_check",
    "tunnel_close",
    "tunnel_create",
    "tunnel_state",
    "tunnel_update",
];
